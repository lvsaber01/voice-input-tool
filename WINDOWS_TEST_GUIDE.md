# Windows 手动测试指引

> **最后更新**: 2026-04-19  
> **适用版本**: v3.1

---

## 前置条件

- Windows 10 1903+ / Windows 11
- Python 3.11+（推荐 3.13）
- 管理员权限的 CMD/PowerShell
- 已连接麦克风
- 项目代码已同步到 Windows（git pull 或复制）

---

## Step 1：安装依赖

打开**管理员** PowerShell，进入项目目录：

```powershell
cd C:\Users\Administrator\PROJECT\voice-input-tool

# 安装基础依赖
pip install -r requirements/base.txt

# 安装 Windows 依赖
pip install -r requirements/windows.txt
```

**预期输出**：所有包 Successfully installed

**验证**：
```powershell
python -c "import faster_whisper, keyboard, sounddevice, pystray; print('OK')"
```

---

## Step 2：下载 STT 模型（首次需要，~500MB）

```powershell
python -c "from faster_whisper import WhisperModel; WhisperModel('small', download_root='./models'); print('Model downloaded')"
```

**预期**：下载 small 模型到 `./models/` 目录，约 500MB

---

## Step 3：启动程序

```powershell
# 必须管理员权限！
python main.py
```

**预期现象**：
1. 控制台输出日志（状态转换、模块初始化）
2. 系统托盘出现圆形图标（灰色=加载中 → 绿色=就绪）
3. 自动打开浏览器或提示模型加载完成

**如果提示"需要管理员权限"**：用管理员 PowerShell 重新运行

---

## Step 4：功能测试

### 4.1 基本语音输入（批量模式）

1. 打开记事本或任意文本编辑器
2. 按 **F8** → 托盘图标变红 + 提示音
3. 对麦克风说话（中文或英文）
4. 再按 **F8** → 图标变橙（识别中）→ 文字自动出现在光标处

**验证点**：
- ✅ 托盘图标颜色切换：绿→红→橙→绿
- ✅ 提示音播放（开始/结束/完成）
- ✅ 文字正确注入到光标位置
- ✅ 右下角出现 VAD 圆形小窗口（颜色随音量变化）

### 4.2 语音命令

在批量模式下说以下词语（**只说命令词，不加其他内容**）：

| 语音命令 | 预期效果 |
|---------|---------|
| 「撤销」 | 撤销上一次输入（Ctrl+Z）|
| 「换行」 | 插入换行 |
| 「句号」 | 插入 。（不是"点"）|
| 「逗号」 | 插入 ，|
| 「感叹号」| 插入 ！|
| 「停止录音」| 停止当前录音 |

### 4.3 静音自动停止

1. 按 F8 开始录音
2. 保持沉默约 8 秒
3. **预期**：自动停止并提示"未检测到有效语音"

### 4.4 录音最大时长

1. 修改 config.yaml：`audio.max_duration: 5`（临时改为 5 秒测试）
2. 按 F8 开始录音
3. 持续说话超过 5 秒
4. **预期**：自动停止并提示"录音已达最大时长 5s"
5. **测试完改回 120**

### 4.5 Web 配置页

1. 右键托盘图标 → "打开设置"
2. 或浏览器访问控制台输出的 URL（含 token）
3. **验证**：
   - 页面加载正常
   - 能看到当前配置
   - 能修改配置并保存
   - 能看到统计面板

### 4.6 声卡设备

1. 在 Web 配置页查看音频设置
2. 如果有多个麦克风，切换设备
3. **验证**：切换后录音仍正常

### 4.7 剪贴板保护

1. 先手动复制一段文字（如 "test123"）
2. F8 录音说话 → 文字注入
3. Ctrl+V 粘贴
4. **预期**：恢复为原来的 "test123"

### 4.8 系统托盘

1. 右键托盘图标
2. **验证菜单项**：
   - 🎤 开始录音 / ⏹ 停止录音（状态切换）
   - 📝 切换到实时转写
   - ⚙ 打开设置
   - 🔍 检查模型
   - ❌ 退出
3. 双击托盘图标 → 打开设置页

---

## Step 5：运行自动化测试

```powershell
python -m unittest discover -s tests -v
```

**预期**：102 tests OK

---

## 常见问题排查

| 问题 | 排查 |
|------|------|
| "需要管理员权限" | 用管理员 PowerShell 运行 |
| "模型加载失败" | 检查 models/ 目录是否有模型文件 |
| 热键无响应 | 检查 keyboard 库是否安装、是否管理员 |
| 麦克风不可用 | 设置→隐私→麦克风→允许 |
| 托盘图标不显示 | 检查 pystray 和 Pillow 是否安装 |
| VAD 窗口不显示 | 仅 Windows 支持，检查 gui/vad_indicator_win32.py |
| 剪贴板注入失败 | 检查是否在管理员权限的程序（如记事本）中测试 |

---

## 测试结果记录

测试完成后，请把结果反馈给 Saber（在飞书群 @Saber）：

```
✅/❌ 依赖安装
✅/❌ 模型下载
✅/❌ 程序启动
✅/❌ 基本语音输入
✅/❌ 语音命令（列出通过的命令）
✅/❌ VAD 指示器
✅/❌ 静音自动停止
✅/❌ 剪贴板保护
✅/❌ Web 配置页
✅/❌ 系统托盘
✅/❌ 自动化测试（102 tests）
遇到的问题：
```
